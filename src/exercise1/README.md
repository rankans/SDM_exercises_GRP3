# Exercise 1 – SQL Recap

This exercise contains SQL queries executed on the IMDB PostgreSQL database
provided via the SDM Docker environment.

## How to run
- Start the Docker container
- Connect via the VSCode PostgreSQL extension
- Execute queries from `queries.sql`
